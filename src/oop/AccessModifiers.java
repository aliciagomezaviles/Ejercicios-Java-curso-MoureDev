package oop;

public class AccessModifiers {

    public static void main(String[] args) {

        // Encapsulamiento

        //  - Modificadores de acceso
        // public (Las clases por buena práctica siempre son públicas)(Se puede acceder desde
        // cualquier parte del programa)
        // private (Se puede acceder solo desde dentro de la clase)
        // protected (Se puede acceder desde la misma clase y subclases)
        // (default) (modificador de acceso por defecto)(Se puede acceder solo desde el mismo paquete)

        // getters (obtener)

        //public String getId() {
        //  return id;
        //}


        // setters (establecer criterios de validación)

        /* public void setAge(int age) {
            if (age > 0) {
                this.age = age;
            } else {
                System.out.println("Edad no válida");
            } */
    }
}
